const button = document.querySelector('#btnComprar');//seleccionamos el boton

button.addEventListener('click', () => { //cuando se haga click en el boton
    alert('Haz hecho click en el boton');
});